# devlog

- shell, landing, studio record + waveform canvas
- own fft, log-mel features, ctc decode, chunked onnx engine
- console with api keys, per-key usage, live key, chart windows + peak markers
- history in indexeddb, themed 404, hero texture, studio hotkeys
- vitest suite, coverage ratchet, ci workflow with coverage artifact + summary
- training toolkit: prepare_data, train, export_onnx, smoke-verified end to end
- real weights bundled: wav2vec2-base-960h int8 re-exported as dragon-stt
- weights installer (url or file), bench with wer/cer + rtf scoring
- mock vad rebuilt percentile-anchored, engine load failures surfaced
- humanization pass: zero comments, lowercase files, orphans deleted
- audit pass: dead ui + deps removed, convex provider off the boot path, themed favicon + manifest, reduced motion, storage guards, fetch timeouts, upload cap
